# Streamlit theme — part of the branding exercise. These are the AI Lab for
# Accountants colors; swap primaryColor (and friends) for your firm's brand
# and the whole app follows.
[theme]
primaryColor = "#16a34a"
backgroundColor = "#f7f6f1"
secondaryBackgroundColor = "#ffffff"
textColor = "#14141b"

[browser]
gatherUsageStats = false
